<div id="header">
    <a href="#" id="sidebarCollapse">
        <i class="fas fa-circle"></i>
    </a>
    <span>
        <a href="{{ path('home') }}" target="_blank"><i class="fas fa-eye"></i> Voir le site</a>
        <span class="divider mr-2 ml-2"> | </span>
        <a href="{{ path('core_clear_cache') }}"><i class="fas fa-sync-alt"></i> Vider le cache</a>
        <span class="divider mr-2 ml-2"> | </span>
        <a href="{{ path('app_logout') }}"><i class="fas fa-power-off"></i> Déconnexion</a>
        {% if core_options %}
            <span class="divider mr-2 ml-2"> | </span>
            <a href="{{ core_options.agencyLink }}">Made with <i
                        class="fas fa-heart"></i>, by {{ core_options.agencyName }}</a>
        {% endif %}
    </span>
</div>
